import NavBar from "./NavBar.jsx";

const Header = () => {
    return (
        <header style={{position: 'fixed', width:'100%'}}>
            <NavBar />
        </header>
    );
};

export default Header;